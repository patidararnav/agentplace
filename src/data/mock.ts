import type { UserLocation } from '@/types';

/* Default map center (Palo Alto) – used when no geolocation is available */
export const defaultUserLocation: UserLocation = { lat: 37.4419, lng: -122.143 };
